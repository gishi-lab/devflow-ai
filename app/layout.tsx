import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "DevFlow AI – Productivity Hub for Indie Developers",
  description:
    "Manage tasks, save notes, store links, and chat with AI — all in one place built for beginner indie developers.",
  keywords: ["developer tools", "productivity", "AI assistant", "task manager", "indie dev"],
  openGraph: {
    title: "DevFlow AI",
    description: "The all-in-one productivity hub for indie developers.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="h-full">{children}</body>
    </html>
  );
}
